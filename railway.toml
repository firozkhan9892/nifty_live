[build]
  builder = "Nixpacks"
  buildCommand = "pip install -r requirements.txt"

[deploy]
  startCommand = "python main.py"
  watchPatterns = ["*.py", "*.pkl", "*.json"]
  healthcheckPath = "/"
  restartPolicyType = "on_failure"
  restartDelay = 10

[service]
  concurrency = 1
